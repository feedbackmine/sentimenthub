<% form_for(@project) do |f| %>
  <%= f.error_messages %>
	
	<%= f.label :name %>
	<%= f.text_field :name %>
	
  <%= f.label :featured %>
	<%= f.check_box :featured %>
	
  <%= f.submit "Create" %>
<% end %>

<%= link_to 'Back', projects_path %>

